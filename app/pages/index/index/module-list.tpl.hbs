<div class="clearfix">
    {{#each modules}}
    <div class="text-center {{../id}}-module" data-index="{{@index}}">{{name}}</div>
    {{else}}
    <span class="text-center">There are no modules.</span>
    {{/each}}
</div>
<div class="clearfix">
    {{#each modules}}
    <div class="text-center {{../id}}-module" data-index="{{@index}}"><i class="status fa {{iconClass}}"></i></div>
    {{else}}
    {{/each}}
</div>